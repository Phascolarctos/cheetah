[package]
name = "cheetah"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
smoltcp = { version = "0.10.0", default-features = true, features = ["log"] }
